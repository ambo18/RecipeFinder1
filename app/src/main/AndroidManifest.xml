<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeFinder"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Recipe_1"
            android:label="Recipe Finder" />

        <activity
            android:name=".Recipe_2"
            android:label="Recipe Finder" />

        <activity
            android:name=".Recipe_3"
            android:label="Recipe Finder" />

        <activity
            android:name=".Recipe_4"
            android:label="Recipe Finder" />

        <activity
            android:name=".Recipe_5"
            android:label="Recipe Finder" />

        <activity
            android:name=".Recipe_6"
            android:label="Recipe Finder" />

        <activity
            android:name=".Recipe_7"
            android:label="Recipe Finder" />

        <activity
            android:name=".Recipe_8"
            android:label="Recipe Finder" />

    </application>

</manifest>